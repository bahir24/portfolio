.header {
  padding-top: 64px;
  display: flex;
  justify-content: space-between;
  position: relative;
  color: #fff;
}

.header__menu {
  flex: 1;
  display: flex;
  justify-content: center;
  margin: 0 20px;
}