.hero {
  height: 100vh;
  min-height: 650px;
}

.hero__container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.hero__content {
  // position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  // align-items: center;
}

.hero__bottom-buttons {
  display: flex;
  justify-content: center;
}