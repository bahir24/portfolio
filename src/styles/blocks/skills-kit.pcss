.skills-kit__title {
  margin-bottom: 35px;
  font-size: 24px;
  font-weight: 700;
  font-family: "Open Sans";
  &::first-letter {
    text-transform: uppercase;
  }
}

.skills-kit__list {
  display: flex;
}

.skills-kit__item {
  margin-right: 40px;
  margin-bottom: 50px;
  position: relative;
}